apiVersion: v1
kind: ConfigMap
metadata:
  name: dchat-dashboards
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  dchat-overview.json: |-
{{ .Files.Get "dashboards/dchat-overview.json" | indent 4 }}
